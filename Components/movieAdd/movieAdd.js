function MovieAddController($scope, $element, $attrs) {
  
}

angular.module('movieApp').component('movieAdd', {
  templateUrl: 'Components/movieAdd/movieAdd.html',
  controller: MovieAddController
});
